language: php
php:
  - '7.0'

install:
 - pear install PHP_CodeSniffer
 - phpenv rehash

before_script:
 - phpenv rehash
 - phpcs -i

script:
 - phpcs --standar=PSR1,PSR2 src/
 - phpcs --report=summary src/
 - phpunit --bootstrap src/autoload.php tests
